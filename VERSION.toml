## VERSION.toml -*- mode: toml-ts-mode -*-
## https://github.com/annie444/version

# Update with:
# fdfind --threads=1 "(pyproject|Cargo).toml" -x version file track {} 'version\s+=\s+"(.*)"'

# Increment with:
# version {level} set +

version = "0.1.0"

[[files]]
file = "addons/jg_asset_packer/plugin.cfg"
expr = 'version="(.*)"'

[package]
